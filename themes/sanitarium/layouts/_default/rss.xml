{{- /* Copied and customized from https://github.com/gohugoio/hugo/blob/master/tpl/tplimpl/embedded/templates/rss.xml */ -}}
{{- $authorEmail := "" }}
{{- with site.Params.author }}
{{- if reflect.IsMap . }}
{{- with .email }}
{{- $authorEmail = . }}
{{- end }}
{{- end }}
{{- end }}

{{- $authorName := "" }}
{{- with site.Params.author }}
{{- if reflect.IsMap . }}
{{- with .name }}
{{- $authorName = . }}
{{- end }}
{{- else }}
{{- $authorName  = . }}
{{- end }}
{{- end }}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = .Site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
{{- $pages = where $pctx.RegularPages "Section" "in" .Site.Params.mainSections }}
{{- else }}
{{- $pages = $pctx.Pages }}
{{- end }}
{{- $limit := .Site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
{{- $pages = $pages | first $limit }}
{{- end }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xml:base="{{ site.BaseURL }}" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ .Site.Title }}{{ end }}</title>
        <link>{{ .Permalink }}</link>
        <description>Recent content {{ if ne .Title .Site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ .Site.Title }}</description>
        <generator>Hugo</generator>
        <language>{{ site.Language.LanguageCode }}</language>{{ with $authorEmail }}
        <managingEditor>{{.}}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>{{ end }}{{ with $authorEmail }}
        <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>{{ end }}{{ with .Site.Copyright }}
        <copyright>{{ . }}</copyright>{{ end }}{{ if not .Date.IsZero }}
        <lastBuildDate>{{ (index $pages.ByLastmod.Reverse 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
        {{- with .OutputFormats.Get "RSS" }}
        {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
        {{- end }}
        {{- range $pages }}
        <item>
            {{- $title := partial "title-with-subtitle.html" . }}
            {{ with $title }}<title>{{ . }}</title>{{ end }}
            <link>{{ .Permalink }}?utm_medium=feed</link>
            <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
            {{- with $authorEmail }}<author>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</author>{{ end }}
            <guid>{{ .Permalink }}</guid>
            {{- /* Plain text image notice for description */ -}}
            {{- $imageNotice := "" }}
            {{- with .Params.images }}
                {{- $imageNotice = printf "[%d image%s, see full post] " (len .) (cond (gt (len .) 1) "s" "") }}
            {{- end }}
            <description>{{ printf "%s%s" $imageNotice (.Summary | plainify | htmlUnescape) }}</description>
            {{- /* Slideshow images as plain <img> for feed readers (keep in sync with image-slideshow.html) */ -}}
            {{- $images := "" }}
            {{- with .Params.images }}
                {{- range . }}
                    {{- $images = print $images `<img src="` (printf "%s%s" site.BaseURL (strings.TrimPrefix "/" .src) | htmlEscape) `" alt="` (htmlEscape .alt) `">` }}
                {{- end }}
            {{- end }}
            <content:encoded>{{ print "<![CDATA[" $images .Content "]]>" | safeHTML }}</content:encoded>
        </item>
        {{- end }}
    </channel>
</rss>
